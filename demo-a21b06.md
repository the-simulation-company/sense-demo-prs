# Buyers think Hugo auto-sends POs when it only drafts them

Copy + visual hierarchy bug, not an autonomy decision — 'ordered' should read 'drafted' until the buyer-approve step lands, and the queue-state badge should be promoted from a low-contrast bottom-corner pill to a top-aligned tag at the row level. PR ships both the copy change and the layout change so the same buyer flow can be trusted at a glance.
